[project]
name = "incident-response-mesh"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "kathan", email = "kathan.ptlkathan1@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "google-adk[db]>=2.9.1",
    "pygithub>=2.10.0",
    "pytest>=9.1.1",
    "python-dotenv>=1.2.3",
    "streamlit>=1.64.0",
]

[project.scripts]
incident-response-mesh = "incident_response_mesh:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
